<!DOCTYPE html>
<html>
    <% include ../partials/head %>
<head>
</head>

<body style="background: #201a4f;">
    <div id="app">
        <% include ../partials/header %>
        <div class="w3-row w3-content">
            <div class="w3-padding w3-white w3-round-xlarge" style="max-width: 400px;margin: 32px auto;/*box-shadow: 0px 0px 25px rgba(255, 255, 255, 0.87);*/">
                <div class="w3-margin" v-if="!reponse">
                    <h1 class="w3-center">{{ quiz.word[0].tm }}</h1>
                    <button class="w3-btn w3-deep-orange w3-margin w3-round-xxlarge" style="width: calc(100% - 32px)" @click="request(0)">
                        {{ quiz.word[quiz.classment[0]].fr }}
                    </button>
                    <button class="w3-btn w3-deep-orange w3-margin w3-round-xxlarge" style="width: calc(100% - 32px)" @click="request(1)">
                        {{ quiz.word[quiz.classment[1]].fr }}
                    </button>
                    <button class="w3-btn w3-deep-orange w3-margin w3-round-xxlarge" style="width: calc(100% - 32px)" @click="request(2)">
                        {{ quiz.word[quiz.classment[2]].fr }}
                    </button>
                    <button class="w3-btn w3-deep-orange w3-margin w3-round-xxlarge" style="width: calc(100% - 32px)" @click="request(3)">
                        {{ quiz.word[quiz.classment[3]].fr }}
                    </button>
                </div>
                <div class="w3-margin" v-if="reponse && !reponse_test">
                    <h1 class="w3-center"><i class="fa fa-close"></i></h1>
                    <button class="w3-btn w3-deep-orange w3-margin-top w3-margin-right w3-margin-left w3-round-xxlarge" style="width: calc(50% - 40px)" @click="show">
                        réponse
                    </button>
                    <button class="w3-btn w3-deep-orange w3-margin-top w3-margin-right w3-round-xxlarge" style="width: calc(50% - 40px)" @click="retray">
                        réessayer
                    </button>
                </div>
                <div class="w3-margin" v-if="reponse && reponse_test">
                    <h1 class="w3-center"><i class="fa fa-check"></i></h1>
                    <div>{{ quiz.word[0].tm }}</div>
                    * <div class="w3-btn w3-round-xxlarge">{{ quiz.word[0].fr }}</div>
                    <h5>example:</h5>
                    <div>
                        <span>{{ quiz.sentence[0].tm }}</span></br>
                        <span class='w3-text-gray'>{{ quiz.sentence[0].fr }}</span>
                    </div>
                    <button class="w3-btn w3-deep-orange w3-margin-top w3-round-xxlarge" style="width: calc(50% - 40px); margin: auto" @click="retray">next</button>
                </div>
            </div>
        </div>
    </div>
    <% include ../partials/footer %>
</body>

</html> 
